# Session working files — regenerated per session, deliberately not tracked
HANDOFF.md

# here.now local claim tokens — never commit
.herenow/

# Fixture corpus and bump-gate scratch — fetched at job time, never vendored
/build/

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
dist/
*.egg-info/
