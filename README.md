# chartagent

An embeddable agentic chart-creation library for Python.

Requires **Python 3.11+**. Alpine / musl is unsupported: duckdb publishes no musllinux wheels.

```bash
uv pip install -e .
```
